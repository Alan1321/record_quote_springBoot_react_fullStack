import React from 'react'
import { Button } from '@mui/material'

import "../styles/main.css"

const Main = () => {
  return (
    <div className="main_container">
        <Button variant="contained" color="success">Add Quote +</Button>
    </div>
  )
}

export default Main